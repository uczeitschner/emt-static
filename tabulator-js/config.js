// holds shared config for all tabulator-js tables

var config = {
    height: 800,
    layout: "fitColumns",
    tooltips: true,
    dataLoader: true,
    pagination: "local",
    paginationSize: 15,
    paginationCounter: "rows"
    };
