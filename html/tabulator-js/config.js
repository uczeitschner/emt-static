// holds shared config for all tabulator-js tables

var config = {
    height: 1000,
    layout: "fitDataStretch",
    tooltips: true,
    dataLoader: true,
    pagination: "local",
    paginationSize: 15,
    paginationCounter: "rows",
    };
